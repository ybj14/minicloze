{
  "name": "minicloze-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android shell for the minicloze PWA (bundled static assets + offline)",
  "scripts": {
    "sync:www": "bash scripts/sync-www.sh",
    "cap:sync": "bash scripts/sync-www.sh && npx cap sync android",
    "cap:open": "npx cap open android",
    "build:sync": "bash scripts/sync-www.sh && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
